<%= provide(:title, 'New Note') %>

<div class="row">
  <div class="span7 offset3">

    <legend>New Note</legend>

    <%= render partial: 'form', locals: {url_path: user_notes_path} %>

  </div>
</div>